{
  "name": "ajo-chain-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply ajo-chain-db --local",
    "migrate:remote": "wrangler d1 migrations apply ajo-chain-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
